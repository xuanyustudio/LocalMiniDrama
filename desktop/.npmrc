# 使用 npmmirror（原淘宝镜像）加速 npm 安装
registry=https://registry.npmmirror.com

# Electron 与 electron-builder 二进制从国内镜像下载（打包时需配合环境变量或 npm run dist:cn）
# 若只在当前项目生效，可在打包前设置：
#   Windows CMD: set ELECTRON_MIRROR=https://npmmirror.com/mirrors/electron/
#   Windows CMD: set ELECTRON_BUILDER_BINARIES_MIRROR=https://cdn.npmmirror.com/binaries/electron-builder-binaries/
# 或直接运行: npm run dist:cn
